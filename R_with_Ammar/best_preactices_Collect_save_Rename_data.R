# 1. Start making a new file with naming convention 
  # > don't use CAPITAL letter
  # > use underscore instead of space
  # > don't start file other than cell A1
  # > you should know your header location
  # > don't use special characters
  # > don't use numbers in the start
  # > don't use spaces in columns names or capital letters
# 2. Use hide any column
# 3. Use similar text style for everything
  # like Pakistan, not PaKISTAN, or pakisTan etc